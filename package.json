{
  "name": "themarvel-cloudflare",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler check",
    "cf:check": "wrangler check",
    "cf:types": "wrangler types",
    "cf:deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply THEMARVEL_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply THEMARVEL_DB --remote",
    "logs": "wrangler tail themarvel-api"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
